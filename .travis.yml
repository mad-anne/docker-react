language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t madanne/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true madanne/docker-react npm run test
